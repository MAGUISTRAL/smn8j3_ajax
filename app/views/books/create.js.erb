alert("le fichier JS est exécuté!");
document.getElementById("book-list").insertAdjacentHTML("beforeend", "<%= j render 'book', book: @book %>")
//trouver la liste qui s'appelle "book-list"
//ajouter une ligne
// permet de rajouter un élément HTML à la fin de cette liste, juste après son dernier enfant (= son dernier <li>
